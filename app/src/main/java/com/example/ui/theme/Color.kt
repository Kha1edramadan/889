package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ── Base surfaces — slightly warmer dark ──────────────────────────────────────
val Surface                 = Color(0xFF0E1010)
val SurfaceContainerLowest  = Color(0xFF090B0B)
val SurfaceContainerLow     = Color(0xFF121515)
val SurfaceContainer        = Color(0xFF181A1A)
val SurfaceContainerHigh    = Color(0xFF202323)
val SurfaceContainerHighest = Color(0xFF2A2D2D)
val SurfaceVariant          = Color(0xFF1D2020)

// ── Text ──────────────────────────────────────────────────────────────────────
val OnSurface        = Color(0xFFF2F1F0)   // brighter white
val OnSurfaceVariant = Color(0xFF555858)

// ── Brand / accent ────────────────────────────────────────────────────────────
val Primary   = Color(0xFFC8FF00)   // electric lime — punchier
val OnPrimary = Color(0xFF101600)

// ── Energy accent — warmth & motivation ───────────────────────────────────────
val Energy    = Color(0xFFFF6B35)   // fiery orange
val OnEnergy  = Color(0xFF2A0D00)

// ── Macro identity colors ─────────────────────────────────────────────────────
val MacroProtein = Color(0xFF60A5FA)   // electric blue
val MacroCarbs   = Color(0xFFF59E0B)   // warm amber
val MacroFats    = Color(0xFFFB923C)   // vivid orange

// ── Semantic extras ───────────────────────────────────────────────────────────
val Positive  = Color(0xFF4ADE80)   // vivid green
val Warning   = Color(0xFFFFB347)   // amber
val Danger    = Color(0xFFFF5C5C)   // red
val InfoBlue  = Color(0xFF60A5FA)   // blue

// ── Outline ───────────────────────────────────────────────────────────────────
val Outline        = Color(0xFF363939)
val OutlineVariant = Color(0xFF262929)

// ── Legacy compat ─────────────────────────────────────────────────────────────
val Error    = Color(0xFFFF5C5C)
val OnError  = Color(0xFF3B0000)
